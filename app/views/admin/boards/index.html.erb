<h1>Boards</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @boards.each do |board| %>
  <tr>
    <td><%= link_to "/#{board.name}/", board %></td>
    <td>[ <%= link_to 'edit', edit_admin_board_path(board) %> ]</td>
    <td>[ <%= link_to 'remove', board, confirm: 'Are you sure?', method: :delete %> ]</td>
  </tr>
<% end %>
</table>

<br />

[ <%= link_to 'create', new_admin_board_path %> ]
